# MoneyMirror Backend Requirements

# Flask (gateway / auth / uploads)
Flask>=3.0.0
flask-cors>=4.0.0
python-dotenv>=1.0.0

# FastAPI (computation engine)
fastapi>=0.115.0
uvicorn>=0.30.0

# Data / ML
numpy>=1.26.0
pandas>=2.2.0

# Database
pymongo>=4.6.0
dnspython>=2.6.1

# Security
Werkzeug>=3.0.0

# HTTP client (Flask proxy to FastAPI)
requests>=2.31.0

# Voice / Audio
faster-whisper>=1.0.0
gunicorn>=21.2.0
python-multipart>=0.0.9
